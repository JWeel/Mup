namespace Mup
{
    public enum FileState
    {
        Unknown,
        SelectFile,
        SelectOption,
    }

    public enum ImageState
    {
        None,
        Loaded,
        Pending,
        Saved
    }
}